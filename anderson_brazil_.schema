default namespace = ""

start =root
 root= element root {(date|quantity|object|action|orginization|location|group)+}
  date= element date {text}
  quantity= element quantity {text}
  object= element object {text}
  action= element action {text}
  orginaization= element orginaztion {text}
  location= element location {text}
  group=element group {text}